# Attribution — frame-ui-terminal-ide

**Original template** authored for content-agent-kit (MIT, same as this repo).
Not vendored from anywhere.

## Design

Pro code editor and IDE frame with file tree sidebar, syntax highlighted code, and terminal build log.

## Use

Slots: `kicker`, `file_name`, `lang_tag`, `code_line_1`, `code_line_2`, `code_line_3`, `code_line_4`, `code_line_5`, `terminal_cmd`, `terminal_output`, `takeaway` — see `../CATALOG.md`.

## Changes in content-agent-kit

None — written here.
